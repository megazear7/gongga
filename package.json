{
  "name": "gongga",
  "version": "0.0.0",
  "description": "A pong like ASCII game",
  "main": "dist/gongga-game.js",
  "bin": "bin/gongga.js",
  "dependencies": {
    "orison": "^1.0.0"
  },
  "devDependencies": {
    "rollup": "^3.17.1"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "play": "node ./cli/cli.mjs",
    "build": "orison build",
    "build:web:component": "rollup --config rollup.config.web.mjs",
    "build:cli": "rollup --config rollup.config.cli.mjs",
    "serve": "orison serve",
    "prod": "NODE_ENV=production orison serve",
    "static": "orison static"
  },
  "author": "Alex Lockhart",
  "license": "ISC"
}
